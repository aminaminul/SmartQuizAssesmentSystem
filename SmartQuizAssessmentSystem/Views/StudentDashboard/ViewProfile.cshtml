@using QuizSystemModel.ViewModels
@model StudentProfileUpdateViewModel

@{
    ViewData["Title"] = "Student Profile";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-success text-white py-3">
                    <h4 class="mb-0 text-center">My Profile</h4>
                </div>
                <div class="card-body p-4">
                    
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success alert-dismissible fade show" role="alert">
                            @TempData["SuccessMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <div class="row mb-4">
                        <div class="col-12 text-center mb-3">
                            <div class="avatar-circle bg-light d-inline-flex align-items-center justify-content-center rounded-circle" style="width: 80px; height: 80px;">
                                <span class="fs-1 text-success">@Model.FirstName.FirstOrDefault()</span>
                            </div>
                            <h3 class="mt-2">@Model.FirstName @Model.LastName</h3>
                            <span class="badge bg-warning text-dark">Student</span>
                        </div>
                    </div>

                    <div class="row g-4">
                        <div class="col-md-6">
                            <h5 class="text-success border-bottom pb-2 mb-3">Personal Information</h5>
                            <div class="mb-3">
                                <label class="small text-muted fw-bold">Email</label>
                                <p class="mb-0 fs-5">@Model.Email</p>
                            </div>
                            <div class="mb-3">
                                <label class="small text-muted fw-bold">Phone</label>
                                <p class="mb-0 fs-5">@Model.PhoneNumber</p>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <h5 class="text-success border-bottom pb-2 mb-3">Academic Details</h5>
                            <div class="mb-3">
                                <label class="small text-muted fw-bold">Education Medium ID</label>
                                <p class="mb-0 fs-5">@Model.EducationMediumId</p>
                            </div>
                            <div class="mb-3">
                                <label class="small text-muted fw-bold">Class ID</label>
                                <p class="mb-0 fs-5">@Model.ClassId</p>
                            </div>
                        </div>
                    </div>

                    <div class="d-grid gap-2 d-md-flex justify-content-md-end mt-4 pt-3 border-top">
                        <a asp-controller="StudentDashboard" asp-action="ChangePassword" class="btn btn-outline-secondary me-md-2"> Change Password </a>
                        <a asp-controller="StudentDashboard" asp-action="UpdateProfile" class="btn btn-primary px-4"> Update Profile </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>